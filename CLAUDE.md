# JRS Digital project memory

- Read the repository `AGENTS.md` instructions and relevant `.agents/skills` before editing.
- Keep the vanilla HTML/CSS/JS architecture; do not introduce a framework.
- Prefer the existing semantic design tokens and maintain WCAG AA contrast.
- For the deals comparison page (`deals/`), see `deals/CLAUDE.md`.
